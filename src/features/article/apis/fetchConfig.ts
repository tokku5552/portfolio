/** Timeout (ms) applied to every external article / OGP request. */
export const FETCH_TIMEOUT_MS = 10000;

/** Maximum number of article pages fetched concurrently for OGP, per source. */
export const OGP_FETCH_CONCURRENCY = 5;
