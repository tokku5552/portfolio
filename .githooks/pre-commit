#!/usr/bin/env bash
set -eu

branch=$(git symbolic-ref --short HEAD 2>/dev/null || echo "")
if [ "$branch" = "main" ]; then
  echo >&2 "ERROR: Direct commits to 'main' are prohibited."
  echo >&2 "       Create a feature branch and open a pull request."
  exit 1
fi
exit 0
